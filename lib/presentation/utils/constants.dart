const kAnimationDuration = Duration(milliseconds: 500);
const kToastDuration = Duration(seconds: 3);

// Seconds to wait in CodeScreen for resend button be available
const kResendSeconds = 60;

// Count of items in one page
const kLimit = 10;

// TODO Spacify app name
const kAppName = '';

// RemoteConfig keys
const kTechnicalWorks = 'technicalWorks';
const kFlutterActualVersion = 'flutterActualVersion';
const kFlutterMinimalVersion = 'flutterMinimalVersion';
